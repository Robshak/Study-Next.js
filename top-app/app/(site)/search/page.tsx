import { Metadata } from "next";

export const metadata: Metadata = {
    title: "search"
};

export default function ContentBySearch(): JSX.Element {
    return (
        <>
            search
        </>
    );
}
